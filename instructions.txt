
0. get source code
	having git installed
	having nodejs installed
1. install packages: open git-bash in project folder "int-quiz", then run:
	npm install react-scripts
2. run project:
	npm start
3. Open http://localhost:3000 to view it in your browser (if neccessary)
